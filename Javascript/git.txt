/* GIT */

What is git?
git is version control tool.

what is version control tool?
version control tool manages the changes in computer programs.

who developed git ?
Git was created by Linus Torvalds in 2005.

Why should we use git?
To version control and manage the changes in computer programs.

Commands :
git clone "Repo-link":- makes local repository of the Project which is in remote repository
git status
git fetch
git add -A 
git commit
git pull 
git push

// To do learn
git stash
git stash pop
git stash list
git stash clear
git stash drop "id"
git branch -a 
git checkout "branchname"
git pull origin master
git revert
